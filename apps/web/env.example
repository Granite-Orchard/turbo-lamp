# API base URL (client-side calls). Must include the global prefix and version.
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:3001/api/core/v1

# Production (Vercel):
# NEXT_PUBLIC_API_URL=https://api.veen.me/api/core/v1
